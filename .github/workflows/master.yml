name: test
on: push
jobs:
  tst:
    runs-on: ubuntu-latest
    steps:
      - uses: jnus/json-variables@v1.0
        with:
          scope: Dev
          configFile: 'tst.json'
          secrets: '${{ toJson(secrets) }}'